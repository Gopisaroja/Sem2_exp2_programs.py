# Sem2_exp2_programs.py
To find the first repeated word in a string.
To print escape characters in Python.
To map function and Lambda expression in Python to replace characters.
To count the number of characters (character frequency) in a string. 
Sample String: google.com'
Expected Result: {'o': 3, 'g': 2, '.': 1, 'e': 1, 'l': 1, 'm': 1, 'c': 1}
To add 'ing' at the end of a given string (length should be at least 3). If the given string already ends with 'ing' then add 'ly' instead. If the string length of the given string is less than 3, leave it unchanged. 
Sample String: 'abc'
Expected Result: 'abcing' 
Sample String: 'string'
Expected Result: 'stringly'
To that takes user’s name and PAN card number as input. Validate the information using isX function and print the details.
To Count Words in Sentence
To Count Uppercase and Lowercase Letters
